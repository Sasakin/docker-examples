pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven("https://oss.sonatype.org/content/repositories/snapshots/")
        maven("https://repo.maven.apache.org/maven2")
        maven("https://plugins.gradle.org/m2")
    }
}

rootProject.name = "cursor-plugin"